# 待修复清单（架构师维护，集成阶段统一派发）

| # | 来源 | 问题 | 归属 | 状态 |
|---|---|---|---|---|
| 1 | 环境工程师 | `fba init` 执行插件 `sql/*/init.sql` 时因文件头 `--` 注释被 FBA SQL 解析拒绝并整单回滚；目前只能用 psql 手工执行。需把 8 份 SQL 文件的前导注释移除或改成 FBA 解析器可接受的形式（对照 `backend/plugin/notice/sql` 的写法与 `backend/plugin/sql.py` 的解析逻辑） | 骨架 | 已修（后端集成）。去掉 4 份 init.sql 文件头 `--` 注释，`parse_sql_script` + 临时库 `fba_sqlcheck` 跑通 8 份 SQL |
| 2 | D3 | D3 自测时曾出现时区问题：订单时刻按 UTC 取时分导致夜间加价误命中，已修为 Asia/Shanghai；集成测试需覆盖「送达 23:05 命中夜间、13:00 不命中」 | 测试 | 已验（T1）。`test_night_window_shanghai_2305_hits_1300_misses` + 流水线 `test_night_surcharge_shanghai_2305_hits_1300_misses`：上海 23:05 / UTC 15:05 命中，上海 13:00 / UTC 05:00 不命中 |
| 3 | 环境 | 多子角色抢占重启 8000 端口；集成阶段串行化，只允许一个角色重启 | 流程 | 已知 |
| 4 | D2 | `GET /riders` 仍挂 `rs:rider:add`，负责人角色有骑手菜单与按钮，暂无影响；F1 若发现负责人 403 再改 | D2 | 观察 |
| 5 | F2 | `POST /plan-versions/{id}/rollback` 应返回新复制的 draft 版本 Detail（含 id），前端据此跳转编辑器；当前前端用 `copied_from_id` 回查兜底 | D3 | 已修（后端集成）。rollback 改为返回新 draft 的 `GetPlanVersionDetail`（含 id） |
| 6 | F2/F1/T2 | 子代理 MCP 会话无法复用父环境浏览器标签；已改由 Playwright 脚本 `scripts/t2_acceptance.mjs`（swagger token 注入 localStorage）完成 A1–A7 + B1–B6，截图 13 张落盘 `docs/验收截图/` | 集成验收 | 已验（T2-Playwright） |
| 7 | F1 | 日标记页每格 `locked` 恒 false：后端 `GET /day-flags?site_id&month` 应逐日返回 `is_locked`（用 `utils/lock_check` 判定所属周期 locked/paid） | D2 | 已修（后端集成）。`GetDayFlagDetail` 增加 `is_locked`，按站点级周期 locked/paid 逐日判定 |
| 8 | F1 | 审计日志 `operator_name` 出现「用户88888」兜底文案，说明 `utils/audit.py` 未正确取到 `request.user.username/nickname`（可能 swagger 登录或 user 对象字段名不同）；需修为优先 nickname、其次 username | 骨架 | 已修（后端集成）。「用户88888」是 FBA 种子 admin.nickname；现优先真实昵称，占位「用户数字」回退 username，admin 写操作显示 `admin` |
| 9 | F1 | `GET /plan-versions/active` 404 时拦截器可能弹 toast（后端已就绪后应不再发生，观察） | 观察 | 观察 |
| 10 | D5 | `DELETE /periods/{id}` 复用了 `rs:period:generate`；决定：init.sql 增加按钮权限 `rs:period:delete`（薪资管理员/负责人均授予），D5 接口改用该码 | 骨架 + D5 | 已修（后端集成）。接口改 `rs:period:delete`；init.sql 增按钮并增量写入现网菜单/角色绑定 |
| 11 | F4 | `GET /payrolls` 缺 `stale` 过滤参数，前端只能过滤当前页 `stale_count>0`；D5 增加 `stale: bool | None` 查询参数 | D5 | 已修（后端集成）。确认 API/CRUD 已支持可选 `stale`，OpenAPI 与 `?stale=true` 200 |
| 12 | F4 | 导出文件名依赖响应头 `Content-Disposition`，FBA CORS 若未 `expose_headers` 则前端回退默认文件名；集成时核对 `backend/core/conf.py` 的 CORS 暴露头配置项，能用 `.env` 配则配，否则前端自行拼文件名（`{站点}_{周期}_薪资明细.xlsx`）作为最终方案 | 集成验收 | 已修（前端集成）：`CORS_EXPOSE_HEADERS` 可由 `.env` 覆盖，已追加 `Content-Disposition`（保留 `X-Request-ID`），需重启后端生效 |
| 14 | F3 | 日历顶部周期 Tag 跳转 `/rider-salary/period?id={period_id}`，F4 周期列表只读 `status/stale` 查询参数；F4 页需支持 `id` 参数：进入后自动打开该周期详情抽屉 | F4 | 已修（前端集成）：周期页消费 `query.id`（缺页则 `GET /periods/{id}` 后按 site 筛选并打开抽屉）；订单/预支页同步消费工作台 `status` |
| 15 | F3 | 周期状态颜色（open 蓝/locked 橙/paid 绿/reopened 紫）写死在 `SummaryBar`，应收敛到 `constants/enums.ts` 并让 F4 周期页与 F3 共用 | 前端集成 | 已修（前端集成）：`PERIOD_STATUS_OPTIONS` 补 color，`SummaryBar` 改 `enumColor`；预支/薪资单/批次本就共用 enums，无重复色表 |
| 13 | D6 | 预支 `mark_paid` 已改为 `mark_stale`；`site_owner_d2` 已补入站点 1 负责人（deputy）。集成用例需覆盖「发放后 `/me/payroll-estimate` 变为实时预估」 | 测试 | 已验（T1）。单测 `test_payroll_estimate.py`；curl D5A001：period1 算薪后 `is_estimate=false`，预支#5 approve→mark-paid 后 payroll#3 draft+stale、`is_estimate=true` |
| 16 | 集成 | `GET /orders/export` 原缺失（D4 记为 D5） | 后端集成 | 已修（后端集成）。补 `GET /orders/export`，中文表头 xlsx，权限 `rs:order:export`（init 已有该码） |
| 17 | 后端集成 | 部分模块自定义审计 `description` 仍可能写入种子昵称「用户88888」（`operator_name` 列已正确为 admin） | 观察 | 观察 |
| 18 | UX-1 | 管理后台列表表格高度异常、会自动缩小：`PageContainer` 的 `p-1` 中间层打断 `Page auto-content-height` 与 VxeGrid `height:'auto'` 高度链；站点列表 query 内 N+1 拉 managers 导致表格 reflow | 前端 UX | 已修（UX-1）。`PageContainer` flex 高度链；站点负责人数列内懒加载；截图 9 张 `docs/验收截图/ux/`；验收脚本 `scripts/ux_check.mjs` |
| 19 | ARCH-1 | draft 重算未回滚预支 remaining，二次算薪丢失抵扣 | 插件 | 已修（ARCH-1）。重算前按旧 advance 明细 restore，再按 cap 重扣 |
| 20 | ARCH-1 | 站点级锁账/解锁会改写已被骑手级周期覆盖的订单/奖惩 `is_locked` | 插件 | 已修（ARCH-1）。站点级跳过重叠的骑手级周期骑手 |
| 21 | ARCH-1 | 方案回退将周期置 reopened 后未解除 `is_locked`，纠错/删除仍被挡 | 插件 | 已修（ARCH-1）。reopen 时调用 `_set_locked_flags(locked=False)` |
| 22 | ARCH-1 | 回退无论是否存在 paid 都强制 `confirm_text`；非 draft 未使用版本编辑文案混用 is_used | 插件 | 已修（ARCH-1）。confirm 仅 paid 强制；非草稿报「仅草稿版本可以编辑」 |
| 23 | ARCH-1 | 产品方案 §3.1 日/周期单量口径与纲要 §10#10 冲突 | 文档 | 已修（ARCH-1）。纲要补遗 #26–#31：枚举落地、单量口径、锁账范围、重算预支、回退确认 |
